# منصة هلا الموحدة v4.9 — Market Builder

## 1) اختبار الواجهة التجريبية بدون Backend
افتح: `offline-demo/index.html`

## 2) التشغيل الحقيقي المحلي (PostgreSQL/PostGIS + Backend + واجهتان)
المتطلبات: Docker + Docker Compose.

```bash
docker compose -f infra/docker/docker-compose.yml up --build
```

العميل: `http://localhost:8088`
الإدارة / منشئ الأسواق: `http://localhost:8089`
Backend: `http://localhost:8080/health`
قاعدة البيانات: `http://localhost:8080/health/db`

## 3) التسلسل
`database/schema.sql` هو المسار القياسي الموحد ويشمل جميع مراحل 001→009. الملفات المرحلية تبقى للمراجعة والترحيل المنفصل فقط.

`database/schema.sql` هو الملف الموحد القابل لإعادة البناء بنفس هذا الترتيب.

## 4) ملاحظة الهوية
نسخة Pilot الحالية تستخدم `X-Actor-Id` لتجربة صلاحيات الإدارة. في الإنتاج يجب استبداله بهوية جلسة/JWT موثقة.

## 5) ملاحظة الإنتاج
لا تعتبر هذه الحزمة إثبات Production نهائيًا حتى يتم تشغيل PostgreSQL/PostGIS فعليًا، واختبار Workers والوسائط والدفع وBackup/Restore والتزامن والأمن وE2E في بيئة تشغيل مخصصة.


## v4.9 — التشغيل الأمني وحوكمة الذكاء
- الدخول الفعلي يعتمد Bearer session بعد OTP، وليس X-Actor-Id.
- بوابة Hala العالمية: `/api/v1/portal`.
- مشرف الذكاء الأعلى: `/api/v1/ai/supervisor/status` للإدارة المخولة.
- ذكاء الأسواق مرتبط بمنظومة `platform_supervisor` ولا يملك صلاحية تحرير المال أو الصلاحيات.
- السوق الجديد يمكن إنشاؤه من السوق المرجعي النشط مع نسخ أقسامه وحقوله وعملياته وإعداداته المنشورة.
- تحقق الدفع محصور بالإدارة/المالية، والعميل لا يستطيع اعتماد عملية دفع لنفسه.


## v4.9 verification
- قاعدة البيانات الموحدة تشمل 009_v4_9_operational_hardening.sql.
- كل سوق جديد يرتبط تلقائيًا بمشرف الذكاء المركزي.
- يمكن تمييز السوق المرجعي النشط من لوحة الإدارة.
- واجهة السوق تستدعي /markets/:market/listings عند توفر بيانات تشغيلية حقيقية.
- لا تعتمد حزمة v4.9 وحدها على وجود PostgreSQL داخل بيئة المراجعة الحالية؛ يجب تنفيذ smoke/E2E على PostgreSQL/PostGIS حقيقي قبل الإطلاق.
